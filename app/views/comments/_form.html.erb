<%= form_for @comment, :url => { :action => :update } do |f| %>
	<p>	
		<%= f.text_area :body, class: "form-control" %>
	</p>

	<%= f.submit "Comentar", class:"large-padding btn btn-primary" %>
<%end%>